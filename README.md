# How to run

```
docker compose up -d --build
docker compose exec sio_test php bin/console doctrine:migrations:migrate --no-interaction
docker compose exec sio_test php bin/console doctrine:fixtures:load
```

### Notes:

- Можно было бы процесс сборки с заполнением базы сделать удобнее, но и так сойдёт для тестового задания
- Не особо заморачивался с обработкой ошибок (что безусловно важно), а надо было бы создать кастомные исключения, унифицировать пул возможных исключений в определенных сервисах и тд
- Не добавлял документацию эндпоинтов, но в реальном проекте, конечно, надо бы. И код ответа не всегда 400 должен быть
- По базе данных - не добавлял индексы, внешний ключ кажется здесь ненужным, но на проде это зависило бы от ситуации и традиций в проекте
- .env не под игнором оказалось) И секреты от БД в docker-compose.yml нельзя хранить
